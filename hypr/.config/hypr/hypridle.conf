general {
    lock_cmd = pidof hyprlock || hyprlock       # avoid starting multiple hyprlock instances.
    before_sleep_cmd = loginctl lock-session    # lock before suspend.
    after_sleep_cmd = hyprctl dispatch dpms on  # to avoid dpms issues after wakeup.
}

# 1. Dim the screen (after 2.5 mins)
listener {
    timeout = 300                                
    on-timeout = brightnessctl set 10%           # set monitor backlight to 10%
    on-resume = brightnessctl set 100%           # restore monitor backlight
}

# 2. Lock the screen (after 5 mins)
listener {
    timeout = 300                                
    on-timeout = loginctl lock-session           # lock screen when timeout has passed
}

# 3. Turn off monitor (after 5.5 mins)
listener {
    timeout = 330                                
    on-timeout = hyprctl dispatch dpms off       # screen off when timeout has passed
    on-resume = hyprctl dispatch dpms on         # screen on when activity is detected
}

# 4. Suspend the PC (after 15 mins)
listener {
    timeout = 900
    on-timeout = systemctl suspend               # suspend pc
}
